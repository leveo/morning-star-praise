fastapi==0.115.12
uvicorn[standard]==0.34.2
python-pptx==1.0.2
python-multipart==0.0.20
Pillow==12.1.1
opencc-python-reimplemented==0.1.7
youtube-transcript-api==1.0.3
yt-dlp==2025.3.31
google-genai==1.14.0
# Optional LLM provider SDKs — install whichever you configure via LLM_*_PROVIDER.
# openai: also covers MiniMax / Qwen / GLM / Ollama via their OpenAI-compatible endpoints.
openai>=1.50.0
anthropic>=0.42.0
psycopg[binary]==3.2.9
aiofiles==24.1.0
faster-whisper==1.2.1
# Sheet-music OMR for the "sheet on PPT" feature (POST /api/sheet/*).
# oemer detects staff-system pixel bounding boxes; model weights (~200 MB,
# 4 ONNX + H5 files) download into site-packages/oemer/checkpoints/ on the
# first /api/sheet/analyze call — trigger manually ahead of time with:
#     python -c "from app.services.sheet_music_service import _ensure_oemer_weights; _ensure_oemer_weights()"
# pdf2image needs the ``poppler`` system package (see README Setup).
oemer==0.1.5
pdf2image==1.17.0
